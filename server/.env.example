PORT=4000

# Optional: if you later switch to a keyed provider like ipgeolocation.io or ipapi.com,
# drop the key here and read it in src/routes/ip.ts. The default provider (ipwho.is)
# needs no key at all.
# IPGEO_API_KEY=
